const maxDimension = 10; // 最大维度显示个数
const maxDimensionColumns = 50; // 最大维度显示数
const maxGroup = 20; // 最大维度
const maxFunnelGroup = 10; // 最大漏斗维度值

export {
  maxDimension,
  maxDimensionColumns,
  maxGroup,
  maxFunnelGroup
}
export default {
  maxDimension,
  maxDimensionColumns,
  maxGroup,
  maxFunnelGroup
}
